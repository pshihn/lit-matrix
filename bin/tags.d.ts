import { Matrix } from './matrix';
export declare function matrix(strings: string[], ...keys: number[]): Matrix;
export declare function meq(strings: string[], ...keys: (number | Matrix)[]): Matrix | number;
